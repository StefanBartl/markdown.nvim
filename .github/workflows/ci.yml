name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    name: tests (headless)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: rhysd/action-setup-vim@v1
        with:
          neovim: true
          version: stable
      - name: Run docs/TESTS suite
        run: nvim --headless -i NONE -u NONE -c "set rtp+=." -c "luafile docs/TESTS/run.lua" -c "qa!"

  lint:
    name: lint (stylua + luacheck)
    runs-on: ubuntu-latest
    # Advisory until a one-time `stylua .` + luacheck cleanup has landed.
    # The `test` job is the authoritative gate.
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: stylua --check
        uses: JohnnyMorganz/stylua-action@v4
        with:
          version: latest
          args: --check .
      - name: luacheck
        run: |
          sudo apt-get update
          sudo apt-get install -y lua-check
          luacheck lua
